var DatabaseController = require('../controller/DatabaseController');
authenticationRoutes = {
  setupRoutes: function(app) {
    // app.get('/database/test', DatabaseController.runTest);
  }
}

module.exports = authenticationRoutes;
